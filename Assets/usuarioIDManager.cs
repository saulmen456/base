using System.Collections;
using System.Collections.Generic;
using System.Data.SqlClient;
using UnityEngine;

public class usuarioIDManager : MonoBehaviour
{
    public int usuarioId { get; private set; } // Propiedad para almacenar el ID del usuario
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
